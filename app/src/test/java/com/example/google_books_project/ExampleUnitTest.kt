package com.example.google_books_project

import org.junit.Test
import org.junit.Assert.*

/**
 * Exemplo de teste unitário local, que será executado na máquina de desenvolvimento (host).
 *
 * Para mais informações sobre testes no Android, consulte a documentação [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {

    /**
     * Teste simples para verificar a soma de 2 + 2.
     * O objetivo é garantir que o resultado da soma seja 4.
     */
    @Test
    fun addition_isCorrect() {
        // Verifica se o resultado da soma de 2 + 2 é igual a 4.
        assertEquals(4, 2 + 2)
    }
}
